import React from 'react';
import Counter from "./counter";

const Profile = () => {
    return (
        <div>
            <h2>Hi there!</h2>
            <Counter/>
        </div>
    );
};

export default Profile;